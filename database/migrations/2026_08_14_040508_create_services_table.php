<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     * Creates the clinic services catalogue table.
     * Each service represents a type of clinical service offered by the clinic.
     */
    public function up(): void
    {
        Schema::create('services', function (Blueprint $table) {
            $table->id();
            $table->string('name');
            $table->text('description')->nullable();
            $table->string('prefix', 10)->unique()->comment('Queue number prefix, e.g. GC for General Consultation');
            $table->unsignedSmallInteger('avg_duration_minutes')->default(15)->comment('Average time per patient in minutes, used for wait estimation');
            $table->boolean('is_active')->default(true)->index();
            $table->timestamps();
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::dropIfExists('services');
    }
};
